# Minishell
This project implements a Linux Mini Shell in C, similar to a basic UNIX shell, where users can execute internal and external commands through a command-line interface. The shell supports features such as process creation, command execution, signal handling, piping, foreground/background job control, and custom shell prompts.
